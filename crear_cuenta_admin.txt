set -a; . ./.env; set +a; ADMIN_EMAIL="admin@tu-dominio.com"; ADMIN_NAME="Admin Global"; ADMIN_PASS="TuClaveSegura"; HASH=$(docker compose exec -T backend node -e "const bcrypt=require('bcryptjs'); bcrypt.hash(process.argv[1],10).then(h=>console.log(h));" "$ADMIN_PASS"); docker compose exec -T db psql -U "$DB_USER" -d "$DB_NAME" -c "INSERT INTO admin_global (email, password_hash, nombre, activo) VALUES ('${ADMIN_EMAIL}', '${HASH}', '${ADMIN_NAME}', true);"
